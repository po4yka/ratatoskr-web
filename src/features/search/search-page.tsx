/**
 * Search arrives with plan item 6. The route exists so the shell, gating,
 * and lazy loading have a first-class surface to hold; its content is the
 * placeholder those slices replace.
 */
export default function SearchPage() {
  return (
    <section>
      <h1 className="text-heading-sm font-semibold">Search</h1>
      <p className="text-body text-muted-foreground">
        Search arrives with the next slice.
      </p>
    </section>
  )
}
